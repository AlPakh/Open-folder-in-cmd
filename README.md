# Open-folder-in-cmd
Скрипт Powershell, добавляющий в контекстное меню папки пункт "Открыть папку в cmd", чтобы не прописывать путь через cd

При некорректном отображении символов кодировки, нужно сохранить скрипт в кодировке UTF-8 с BOM (например через Notepad++) и перезапустить скрипт
